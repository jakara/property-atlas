{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; [[ \"$f\" == *.swift ]] && swiftformat \"$f\" 2>/dev/null && swiftlint lint \"$f\" 2>/dev/null || true; }",
            "timeout": 30,
            "statusMessage": "SwiftFormat + SwiftLint..."
          }
        ]
      }
    ]
  }
}
